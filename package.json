{
  "name": "servicenow-client-global-dts",
  "version": "0.1.0",
  "description": "JavaScript Typings for globally-scoped ServiceNow client-side development",
  "main": "server.js",
  "author": {
    "name": "Leonard T. Erwine"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@types/node": "^8.0.14",
    "typescript": "^3.2.2"
  }
}
